# PaneCue v0.1 Acceptance Gate

Status: **v0.1 local acceptance complete on macOS 26 Apple Silicon**

Run the complete automated gate from the repository root:

```sh
./scripts/run_v01_acceptance.sh
```

The command runs the Swift acceptance and regression suites, packages the
stable application, verifies its signature and release identity, confirms
that experimental permission descriptions are absent, and checks the bundled
offline parser against its release size budget.

## Frozen-case coverage

| Frozen case | Automated coverage | Remaining system smoke |
|---|---|---|
| New install → Accessibility → Preview → Apply → Undo | Preview, result, and reversible transaction models | Passed after a clean Accessibility reset on macOS 26 in the 2026-07-26 smoke run |
| Russian three-window command | `V01AcceptanceTests` | Passed with reference apps on macOS 26 in the 2026-07-26 smoke run |
| English three-window command | `V01AcceptanceTests` | Passed with reference apps on one physical display on macOS 26 in the 2026-07-26 smoke run |
| Follow-up resize before Apply | `V01AcceptanceTests` and `WorkspacePlanTests` | None |
| Mouse resize before Apply | `V01AcceptanceTests` and grid tests | Passed with a corner drag, snapped Preview, and matching real-window Apply on macOS 26 in the 2026-07-26 smoke run |
| Ambiguous target | `WorkspaceApplyPreflightTests` | Passed with three matching windows on macOS 26 in the 2026-07-26 smoke run |
| Closed application offered for launch | Launch remains gated behind Apply | Passed with a packaged-app launch on macOS 26 in the 2026-07-26 smoke run |
| Target closes between Preview and Apply | Missing target and late-close branches are covered | Passed with a real target closed immediately before Apply on macOS 26 in the 2026-07-26 smoke run |
| Non-resizable partial result and Rollback | Preflight and result aggregation are covered | Passed with a constrained real window on macOS 26 in the 2026-07-26 smoke run |
| Full-screen and minimized targets | Window-state preflight and partial result aggregation are covered | Passed on one physical display on macOS 26 in the 2026-07-26 smoke run |
| External display disconnects after Preview | Display condition revalidation and partial result aggregation are covered | Passed with Sidecar on macOS 26: the external target was skipped without fallback and Rollback restored the available target |
| Complete offline flow | Stable profile and local parser are verified | Passed in Local only mode on macOS 26: Preview → Apply → Undo completed with zero process network sockets |
| Save Cue, relaunch, and apply | Archive and plan recreation are covered | Passed on macOS 26 in the 2026-07-26 smoke run |
| Import and export Cue | Versioned archive round-trip and validation | Passed with native Open/Save panels on macOS 26 in the 2026-07-26 smoke run |

## Manual macOS smoke procedure

Use [the support matrix](v0.1-support-matrix.md) and record the macOS version,
Mac model, display configuration, applications, pass/fail result, and issue
link for every failure. Do not add window titles, commands, URLs, or document
paths to the record.

For every layout smoke case, record the expected normalized position and size
from Preview, then verify the applied X, Y, width, and height for every target.
An outcome label such as `Moved` is not sufficient when the Preview geometry
itself is wrong.

The local v0.1 milestone is closed when every case in the supported macOS 26
Apple Silicon scope passes on the packaged `PaneCue.app` and is recorded.
Public binary distribution has a separate deferred signing and notarization
gate.

Recorded system runs:

- [macOS 26 core smoke — 2026-07-26](v0.1-smoke-2026-07-26.md): stable
  launch, three-window complete Apply and Undo, three-window partial Apply and
  Rollback, two-window complete Apply and Undo, saved-Cue relaunch and Apply,
  native Cue export/import, Apply-gated application launch, a target closed
  between Preview and Apply, and ambiguous-target handling passed on Apple
  Silicon. Full-screen and minimized targets also produced the expected
  readable partial results without being moved, and Rollback restored the
  available target. A clean Accessibility reset and a Local only
  Preview → Apply → Undo pass with process-level network monitoring also
  passed. A Sidecar extended display also passed cross-display Apply and Undo,
  plus disconnect-after-Preview handling with a successful Rollback. This
  closes the supported macOS 26 Apple Silicon scope; macOS 14, macOS 15, and
  Intel qualification are explicitly deferred.
