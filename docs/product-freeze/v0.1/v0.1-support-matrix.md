# PaneCue v0.1 Support Matrix

Status: **Frozen target; qualification incomplete**

“Target” means the combination is part of the release contract. It becomes
“Verified” only after the named acceptance matrix passes on a packaged,
signed beta build.

## Platforms

| Area | Status | Contract |
|---|---|---|
| macOS 14 | Target | Minimum supported version in `Package.swift` and `Info.plist` |
| macOS 15 | Target | Must pass full reference-app matrix |
| macOS 26 | Target | Must pass full reference-app matrix |
| Apple Silicon | Target | Primary public-beta architecture |
| Intel | Not supported | No binary until separately built and qualified |
| Built-in display | Target | Full v0.1 support |
| One external display | Limited | Explicit target placement; full test matrix required |
| More than two displays | Experimental | No release guarantee |
| Current Space | Target | PaneCue operates only in the current Space |
| Move between Spaces | Not supported | PaneCue does not move windows between Spaces |
| Stage Manager | Limited | No correctness guarantee in first beta |

## Languages

| Surface | Support |
|---|---|
| Application UI | English |
| Text commands | Russian and English |
| Offline speech | Russian and English, optional |
| Documentation | Russian and English |

## Reference applications

| Application | v0.1 target | Notes |
|---|---|---|
| Safari | Supported target | Browser role and generic window movement |
| Google Chrome | Supported target | Generic browser windows only; no video-tab automation |
| Finder | Supported target | Generic window movement |
| Terminal | Supported target | Generic window movement |
| iTerm2 | Supported target | Generic window movement |
| Notes | Supported target | Notes role |
| Visual Studio Code | Supported target | IDE role |
| Xcode | Supported target | IDE role |
| Slack | Supported target | Generic window movement |
| Discord | Supported target | Generic window movement |
| Zoom | Supported target | Ordinary resizable window only |
| FaceTime | Limited target | Ordinary resizable window only; no capture or extraction |

The code recognizes additional applications and roles, but they are not
officially supported until they are added to this table and qualified.

## Window behavior

| Window state | Status | Behavior |
|---|---|---|
| Normal resizable window | Supported target | Preview, Apply, and Undo |
| Non-resizable or system window | Limited | Skip and explain |
| Full-screen window | Limited | Never leave full-screen without confirmation |
| Minimized window | Pending qualification | Do not promise support until tested |
| Window closed after Preview | Supported failure path | Revalidate before Apply |
| Unsaved document | Protected | PaneCue never closes or terminates the app |
| Multiple matching windows | Supported failure path | User chooses from candidates |

## Display limitations

- PaneCue uses the visible work area and avoids the Dock and menu bar.
- An external display target falls back to the main display in the current
  prototype; the beta must instead explain a missing display unless the user
  explicitly approves fallback.
- Display disconnection between Preview and Apply must block or recalculate
  the plan.
- Different scaling, Dock positions, and menu-bar ownership must be covered by
  the display matrix.

## Qualification matrix

Each supported macOS version must pass:

1. install and Accessibility onboarding;
2. inventory of one and multiple windows per reference app;
3. Preview fidelity;
4. move and resize;
5. three-window Apply;
6. Undo;
7. app relaunch after a saved Cue;
8. full-screen and minimized failure behavior;
9. one external display where applicable;
10. offline execution with network disabled.

No application can be labeled “Supported” in public documentation until its
matrix passes on at least one supported macOS version and no P0/P1 issue
remains.
